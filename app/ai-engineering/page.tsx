import type { Metadata } from "next";
import { PageIntro } from "@/components/PageIntro";
import { aiUses, aiWorkflow } from "@/content/site-content";

export const metadata: Metadata = { title: "AI Engineering", description: "How Jason Pollard uses AI safely and productively in real software delivery.", alternates: { canonical: "/ai-engineering" } };
export default function AiEngineeringPage() { return <><PageIntro eyebrow="AI engineering" title="AI accelerates the work. It does not own the decision."><p>I use GitHub Copilot in VS Code for production engineering and OpenAI Codex for independent work. Repository instructions, reusable prompts, structured handoffs, and custom tools make the work repeatable—not automatic.</p></PageIntro><section className="principle-band"><div className="shell"><p>Operating principle</p><blockquote>Generated output is a draft, not a decision.</blockquote></div></section><section className="section shell split"><div className="section-heading"><p className="eyebrow">Applied, not ornamental</p><h2>Useful across the delivery path.</h2><p>AI is most valuable when it is grounded in the codebase, contracts, constraints, and evidence already present.</p></div><ul className="uses-list">{aiUses.map((item) => <li key={item}>{item}</li>)}</ul></section><section className="section evidence-section"><div className="shell"><div className="section-heading"><p className="eyebrow">Review loop</p><h2>A disciplined path from context to confidence.</h2></div><ol className="workflow-grid">{aiWorkflow.map((item, i) => <li key={item}><span>{String(i + 1).padStart(2, "0")}</span>{item}</li>)}</ol></div></section><section className="section shell"><article className="spike"><p className="eyebrow">Example: feature-flagged UI spike</p><h2>Turn an underspecified integration into testable work.</h2><p>For a production UI integration spike, I broke an ambiguous story into explicit steps and open questions. AI helped inspect API shapes, exercise parameter combinations, and validate data paths involving APIs and Azure Blob Storage before implementation.</p><p>The value was not a burst of generated code. It was faster clarification, broader validation, and a smaller set of unknowns entering implementation.</p></article></section></>; }
